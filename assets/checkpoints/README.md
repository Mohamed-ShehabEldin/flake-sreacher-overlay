# SAM2 checkpoint

`deploy_flake_searcher.py` installs the pinned `sam2.1_hiera_small.pt`
checkpoint here for full training setups. The checkpoint is generated locally,
verified by size and SHA-256, and intentionally excluded from Git.
